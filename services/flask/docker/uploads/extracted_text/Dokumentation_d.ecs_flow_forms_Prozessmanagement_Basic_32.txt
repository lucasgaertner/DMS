d.vetop
academy
Tutorial d.ecs forms basis

// The formInstanceId will be set when the proposal was started via intranet or
// process protal
vars formInstanceId = dok_dat_feld[30]

// STEP 2: Set the formInstanceId if not set already
if( formInstanceId == "" )
{
1UUID = sm_sdtime( "n%0d%0m%4y-%0h%0M%0s" ) // get a current timestamp -----

AL MS PUGET SS IR Trac MRT NTEIE CU B04
formInstancelId = "wfl/" ## doku_id ## "/" ## lUUID
}

// STEP 4: Make them global for the complete workflow -------------------------
call save_vars( "formInstanceId" )

call save_vars( "wSuperior" )

call save_vars( "wApplicant" )

call save_vars( "wStepID" )

call save_vars( "wState" )

Antrag genehmigen

 

Anhand des hinterlegten Vorgesetzten in der Dokumentart ,,Urlaubs-Antrag” erfolgt hier die
Wiedervorlage des ,,User-Interfaces”. Der Priifer bekommt die Mitarbeiter- und die Urlaubs-Daten
angezeigt, kann diese aber nicht mehr editieren. Er hat die Médglichkeit einen Kommentar
einzugeben, hat Zugriff auf die Notiz-Eintrage und kann den Urlaubsantrag Uber eine Radio-Box
genehmigen oder ablehnen; diese Option der Bearbeitung hat nur der Vorgesetzte.

Switch

 

Abhangig von der Entscheidung des Vorgesetzten wird bei ,Genehmigung” die Personal-Abteilung
informiert, und es werden dann die genehmigten Urlaubstage von den zur Verfligung stehenden
Urlaubstagen in der Personal-Akte abgezogen. Ist der Urlaubsantrag ,Abgelehnt” erfolgt nur eine
Information an den Antragsteller.

Personal-Abteilung

 

Die Personal-Abteilung bekommt den Antrag Uber das User-Interface nur zur Ansicht und kann keine
Daten im Interface selbst andern.

[Optional ware hier noch eine Ablehnung und demzufolge eine Ruickmeldung an den Antragsteller
mdglich]

Urlaubsdaten verbuchen

 

Die Urlaubsdaten miissen entweder in einem separaten Schritt oder innerhalb des Schrittes
,Personal-Abteilung” mit den Daten der Personal-Akte verrechnet und aktualisiert werden.

Thema - Urlaubs-Antrags Workflow
33/81 d.velop