d.vetop

academy
Tutorial d.ecs forms basis

6.5. Daten-Modell

Da die Verwendung von d.ecs forms auch Uberlegungen uber das benédtigte Daten-Modell
voraussetzt, muss dieses auch im Rahmen der Spezifikation tberdacht und geplant werden. Hier sind
folgende Bereiche zu berticksichtigen:

Mittels d.ecs-forms kénnen diese Datenobjekte im Vorfeld der Workflow-Erstellung separat angelegt
und damit auch schon, in der unten dargestellten Form, in die Spezifikation aufgenommen werden.

Objekt-Modell / Liefernde Datenquellen

Die Mitarbeiter-Daten werden tber ein DB-Statement aus der d.3-Datenbank ermittelt. Basis hierfur
ist die Aktenart ,,Personal-Akte”.

 

@ EmployeeData

    
 
 

é personallD

Ea} O firstName

EmployeeDataSQL @ loginName

  

 
 

EmployeeDB

@ superior

@ surName

@ unusedVacationdays

 

 

 

Abbildung 6.2 Daten-Modell Mitarbeiter-Daten

Die Daten werden mittels eines einfachen SQL-Skriptes aus der d.3-Tabelle ermittelt.

SELECT dok_dat_feld_2 AS 'loginName', dok_dat_feld_4 AS 'surName',
dok_dat_feld_3 AS 'firstName', dok_dat_feld_5 AS 'superior',
dok_dat_feld_80 AS 'unusedVacationDays'

FROM dbo.firmen_spezifisch
WHERE kue_dokuart = 'APERS' AND dok_dat_feld_1 = ${personalID}

Thema - Urlaubs-Antrags Workflow

36/81 d.velop