d.ve.op

academy
Tutorial d.ecs forms basis

12. Performance

 

12.1. save vs. savelnstance

 

 

Abbildung 12.1 savelnstance vs. save

   

 

 

 

Die Inhalte des Formulars k6nnen entweder

e ,nur" in der d.ecs forms-Datenbank gespeichert werden (savelnstance), damit ist sichergestellt,
dass die Inhalte im nachsten Schritt wieder dargestellt werden kénnen; oder

e auch in die externen Datenquellen (save) wie angeschlossene Daten-Objekte, in denen das Save-
Skript vorhanden ist, Dokument-Eigenschaften und / oder den Workflow-Variablen

gesichert werden.

A Tae TR
form.ui.setEnabled( "btn_Save", false );

 

// STEP 2: Save data

// Success-Callback

var onSaveSuccess = function onSaveSuccess () {
form.ui.setEnabled( "btn_Save", true );

Mi

// Exrror-Callback
var onSaveFailure = function onSaveFailure( errorCode, errorMessage) {
if ( errorCode == -3) {
// There is a conflict with the db. You need to merge instead of alert.
form.notification.alert( "Unable to save the context since another user has saved the same
instance in between.", "Save Failed" );
} else {
form.notification.alert( "Unable to save the context: " + errorMessage, "Save Failed" );

15/81 d.velop

Thema - Performance