d.ve.op
academy
Tutorial d.ecs forms basis

Damit die Eigenschaften gelesen und unabhangig vom Dokumentstatus auch geschrieben werden
kénnen, bendtigt der User ALLE Rechte auf die Dokumentklassen, welche im Workflow verarbeitet
werden!

11.6.3. Im Workflow / d.ecs forms-Editor

Uber den Formular-Kontext und dort die ,Erweiterten Eigenschaften” muss dieser User auch
registriert werden.

Erweiterte Einstellungen

Nehmen Sie hier die erweiterten Einstellungen des Forrnular-Kontextes vor

 

 

Cache

{-] 4.3 Dokument-Eigenschaften beim Laden irnmer aus d.3 ermitteln

d3 Systembenutzer

Name dEcsFormsUser

Abbildung 11.2 Erweiterte Einstellungen - d.3-Systembenuzter

 

11.7. | Globale Skripte nutzen

Innerhalb des Formulars kann eine eigene JavaSkript-Bibliothek hinterlegt werden. Dazu k6nnen im
Bereich ,,Globales Skript” einzelne Funktionen definiert werden.

In diesem Fall wird die Berechnung des Brutto-Betrags inkl. einer Funktion zum kaufmannischen
Runden als globale Funktionen definiert.

// veturns gross value --------------------------------------------------------------
form.global.calcBrutto = function( pNetValue, pTaxValue ) {

var tmpValue = 0;
// Set Brutto-Value
if( pNetValue != null && pTaxValue != null ) {

tmpValue = new Number( pNetValue ) *
new Number( 1 + new Number( pTaxValue ) / 100 );
}
return form.global.myRound( tmpValue );
}#// end of calcBrutto

// Returns rounded value ------------------------------------------------------------
form.global.myRound = function( pInputValue ) {

// Calculate the right amount

var x = pInputValue;

var k = (Math.round(x * 100) / 100) .toString();

Thema - Vermischtes
73/81 d.velop